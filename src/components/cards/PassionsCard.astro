---
import Card from "../Card.astro";
import CustomIcon from "../CustomIcon.astro";
import { getMyInfo } from "@/utils";
const my_info = await getMyInfo();
---

<Card title="PASSIONS">
    {my_info.passions.map((passion) => (
      <div style="display: flex; align-items: center; gap: 10px;">
        <CustomIcon name="mdi:heart" size="26" />
        <span class="font-weight-bold normal-text">{passion}</span>
      </div>
      <hr class="dashed-line" />
    ))}
</Card>
