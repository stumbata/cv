---
import Card from "../Card.astro";
import { getMyInfo } from "@/utils";
const my_info = await getMyInfo();
---

<Card title="COURSES">
    {my_info.courses.map((course_title) => (
      <div class="course-title">{course_title}</div>
      <hr class="dashed-line" />
    ))}
</Card>
